@page
@model DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<div class="main-container">
    <div class="main-header">
        <h1>@ViewData["Title"]</h1>
        <div class="main-actions"></div>
    </div>
    <div class="main-content">

        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.MeterReadingType.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.MeterReadingType.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.MeterReadingType.Unit)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.MeterReadingType.Unit)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="MeterReadingType.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-page="./Index">Back to List</a>
        </form>
    </div>
</div>
